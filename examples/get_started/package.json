{
  "name": "stan-wasm-rs-get-started",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-wasm": "mkdir -p public && cp ../../ts/pkg/stan_wasm_api_bg.wasm public/",
    "dev": "npm run copy-wasm && vite",
    "build": "npm run copy-wasm && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "stan-wasm-rs": "file:../../ts"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  }
}
