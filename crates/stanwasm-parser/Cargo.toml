[package]
name = "stanwasm-parser"
description = "Hand-written recursive-descent parser for a subset of the Stan language. Internal to stanwasm; no API stability guarantee."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["stan", "parser", "lexer", "bayesian", "probabilistic"]
categories = ["parser-implementations", "science"]

[dependencies]
stanwasm-ast.workspace = true
thiserror.workspace = true
